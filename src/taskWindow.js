const TaskWindow = () => {
// Create a popup for full detailed task
// Project
// task
// priority
// due date
// notes
const mainContainer = document.querySelector('#todo-container');

const iTaskContainer = document.createElement('div');
const projectContainer = document.createElement('div');
const projectHeader = document.createElement('div');
const projectBody = document.createElement('div');

mainContainer.appendChild(iTaskContainer);
iTaskContainer.appendChild(projectHeader);
iTaskContainer.appendChild(projectBody);

return {}
}

export default TaskWindow
